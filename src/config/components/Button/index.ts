import styled from '@emotion/styled';

const Button = styled.div({
  borderRadius: 5,
  backgroundColor: '#4a4a4a',
  color: '#ffffff',
  margin: '0 5px',
  padding: 5,
  lineHeight: '18px',
  cursor: 'pointer',
});

export default Button;
