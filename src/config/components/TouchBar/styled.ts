import styled from '@emotion/styled';

export const Wrapper = styled.div({
  width: '100%',
  textAlign: 'center',
  marginBottom: 40,
});

export const TouchBar = styled.div({
  height: 40,
  backgroundColor: '#1f1f1f',
  width: '80%',
  margin: '0 auto',
  minWidth: 900,
  borderRadius: 5,
  padding: 6,
  display: 'flex',
});
